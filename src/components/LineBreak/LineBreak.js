import LineBreakWrapper from "./styles";

const LineBreak = ({ name }) => {
    return (
        <LineBreakWrapper>
            <h3>{name}</h3>
        </LineBreakWrapper>
    );
};

export default LineBreak;
